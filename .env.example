# Server Configuration
PORT=3011

# Database (optional, defaults to ~/.agent-os/data.db)
# DB_PATH=~/.agent-os/data.db

# SSH config for "Open in VS Code" remote button (optional)
# SSH_HOST=myserver.example.com
# SSH_PORT=22
